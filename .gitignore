# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.DerivedData*/
build/

# Swift Package Manager
.build/
.swiftpm/

# Prebuilt binary dependency — fetch with Scripts/fetch-sherpa-onnx.sh
sherpa-onnx.xcframework/
dist/

# Profiling / tooling
*.profraw

# Local agent tooling never ships publicly
.claude/
Scripts/codex-ask.sh
Scripts/codex-exec.sh

# Per-machine signing overrides (referenced by Base.xcconfig)
Local.xcconfig

# Local agent instructions (workflow and identity preferences stay off the public repo)
CLAUDE.md

# Internal working documents (audits, plans, bug reports) never ship publicly;
# screenshots stay tracked
docs/*.md
